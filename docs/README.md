# docs/ index

This repo currently covers Stages 1–2 only (data collection + dataset build). All prior
modeling-research docs (eiie_agent/, m_series/, risk_mandate/, h_series/, conviction_model/,
notes.md, specification.txt, RESEARCH_REFERENCES.md, FEATURE_SCALING_AUDIT.md, papers/) were
deleted in the 2026-07-23 factory reset described in `CLAUDE.md`'s "Stage 3+: Modeling" section —
recoverable via `git log` on `refactor` before that date, but not reused going forward without
deliberately re-reading it.

No docs subdirectories remain here yet. Future modeling work should add its own plan doc(s) as it
starts, per `CLAUDE.md`'s "Persist plans as repo files" convention.
