yfinance==0.2.66
pandas==2.3.3
plotly==6.3.1
python-bcb==0.3.3
numpy==2.2.6
pytz==2025.2
python-dateutil==2.9.0.post0
httpx==0.28.1
certifi==2025.8.3
charset-normalizer==3.4.3
idna==3.10
urllib3==2.5.0
# Stage 2 preprocessing
scikit-learn==1.9.0
joblib==1.5.3
# Stage 3 portfolio (src/portfolio/) -- scipy.stats for the deflated Sharpe ratio
scipy==1.18.0
# Stage 3 portfolio -- the cost-aware convex optimizer (proposal §5)
cvxpy==1.7.4
# Stage 3 portfolio -- the Stage A alpha forecaster (proposal §2b)
lightgbm==4.6.0
# Stage 3 portfolio -- renders a single tree from the alpha model (plot_tree.py); needs the
# system `dot` binary too (already present here, install via your OS package manager otherwise)
graphviz==0.21
# Testing
pytest==8.3.4
ruff==0.15.20
coverage==7.6.9  # `python tests/run_all.py --coverage` -- informational src/ coverage report
